import React from 'react'
import { Button } from 'semantic-ui-react'

const ToggleButton = ({Color, currentColor, onChangeColor}) => (
  <Button.Group>
    
  </Button.Group>
)

export default ToggleButton
